= content_for :splash_nav do
  %li
    = link_to('Tickets', conference_buytickets_path(@conference.short_title), :class=>"smoothscroll hidden-xs")
    = link_to('Tickets', conference_buytickets_path(@conference.short_title), :class=>"smoothscroll visible-xs", 'data-target'=>".navbar-collapse", 'data-toggle'=>"collapse")
